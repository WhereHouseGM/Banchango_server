spring.jpa.show-sql=false
#logging.level.org.hibernate.SQL=DEBUG
server.tomcat.uri-encoding=UTF-8
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.open-in-view=false
spring.datasource.url=${TEST_WH_DATASOURCE_URL}
spring.datasource.username=${WH_DATASOURCE_USERNAME}
spring.datasource.password=${WH_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
aws.access_key_id=${AWS_ACCESS_KEY_ID}
aws.secret_access_key=${AWS_SECRET_ACCESS_KEY}
aws.s3.bucket=${AWS_S3_BUCKET}
aws.s3.region=${AWS_S3_REGION}
banchango.no_image.url=${WH_DEFAULT_IMAGE_URL}
banchango.email=${BANCHANGO_EMAIL}
banchango.email.password=${BANCHANGO_EMAIL_PASSWORD}
bancahng.email.debug=${BANCHANGO_EMAIL_DEBUG}